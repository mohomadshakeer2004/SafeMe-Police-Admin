/**
 * Local dev only — copy this file to firebase-config.local.js (gitignored).
 *
 * Option A (recommended): In Google Cloud, add HTTP referrers for your dev URL
 *   http://localhost:8080/*
 *   http://127.0.0.1:8080/*
 * to API key in firebase-config.js (admin browser key)
 *
 * Option B: Create a separate Browser API key with Application restrictions = None
 * (dev only), then uncomment and paste it below:
 */
// window.SAFEME_FIREBASE_CONFIG_OVERRIDE = {
//     apiKey: "YOUR_DEV_BROWSER_API_KEY",
// };
